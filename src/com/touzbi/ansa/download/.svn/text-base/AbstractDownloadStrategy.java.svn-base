package com.touzbi.ansa.download;

import java.util.Arrays;

public abstract class AbstractDownloadStrategy implements DownloadStrategy {
	@Override
	public void download(String... urls) {
		download(Arrays.asList(urls));
	}
}
